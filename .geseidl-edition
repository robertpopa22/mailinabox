# Geseidl Edition overlay manifest.
# Prezenta acestui fisier = overlay activ. Vezi management/geseidl_edition/OVERLAY.md
edition: geseidl
overlay_version: 0.10.1
# 0.10.1: status upstream compara commitul local cu upstream/main si semnaleaza explicit restanțele de securitate
# 0.10.0: zona mail — procesare rua in gesit-alerte@; dit@ notificat numai la rezultate DMARC problematice
# 0.9.0: zona mail — restrictie acces IMAP per-cont la source IP (Dovecot allow_nets, tabel sidecar + CLI imap_restrict.py)
# 0.8.0: zona spam completa — patch-uri integrare extrase+verificate (mail-postfix/spamassassin/daemon-api/index) + apply.sh signature-gate
upstream_base: v75
zones:
  - status
  - dns
  - ssl
  - mail
  - web
  - spam

